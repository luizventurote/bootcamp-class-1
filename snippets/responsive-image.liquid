<picture style="display: block; width: 100%;">

    <source media="(max-width: 749px)" data-srcset="{{ mobile_image }}" />

    <img src="{{ small_image }}" 
    data-src="{{ desktop_image }}" 
    alt="{{ alt_image }}" class="lazyload"/>

</picture>